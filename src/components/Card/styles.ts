import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  flex-direction: column;
  gap: 5px;
  width: 150px;
  text-align: center;
`;

export const Image = styled.img`
`;

export const Title = styled.abbr`
  font-weight: bold;
  color: #000;
  font-size: 14px;
`;

